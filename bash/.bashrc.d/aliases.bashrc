# bash aliases

alias rebash=". ~/.bashrc"
alias rmd="rm -r"
alias histg="history | grep"
alias chmod?="stat -c %a"
alias psg="ps aux | grep -v grep | grep -i -e VSZ -e"
alias sysup="sudo apt update  && sudo apt upgrade"
